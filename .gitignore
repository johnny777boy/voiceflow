# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Worktrees (managed by dev workflow)
.worktrees/

# macOS
.DS_Store

# Xcode (not used, but ignore just in case)
*.xcodeproj/xcuserdata/
DerivedData/

# Build artifacts / app bundle output
dist/
*.app

# Secrets
*.env
secrets/

# Machine-local Claude session state
.claude/

# Verbatim dictation text — never leaves the Mac.
docs/bad-dictations.jsonl
bad-dictations.jsonl

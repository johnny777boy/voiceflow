import Foundation
import VoiceFlowCore

// Placeholder entry point. The full SwiftUI menu-bar application is assembled in
// AppMain.swift (added once platform implementations land). Kept minimal so the
// package always compiles during incremental development.
print("VoiceFlow \(VoiceFlowInfo.version)")
